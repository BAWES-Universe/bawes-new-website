# Bot Memory

> Per-bot persistent memory across conversations.

**Status:** 🟢 LIVE

Each bot has its own memory that persists across sessions. Stores conversation history, user interactions, learned facts. Memory loaded when bot spawns, restored on space join. Enables coherent long-term conversations.